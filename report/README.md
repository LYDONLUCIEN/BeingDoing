# PDF 导出工具使用说明

这个工具可以将 KATE优势身份证 HTML 页面完美导出为 PDF 文件，保留所有样式、渐变和布局。

## 📦 快速开始

### 1. 安装依赖

在 `port/` 目录下执行：

```bash
npm install
```

### 2. 安装浏览器（首次使用）

```bash
npm run install:playwright
```

> ⚠️ **注意**：首次运行会自动下载 Chromium 浏览器（约 150MB），需要等待一段时间

### 3. 导出 PDF

**导出单个 PDF（A4 纵向）：**

```bash
npm run export
```

**批量导出多种格式：**

```bash
npm run export:batch
```

## 🎯 高级用法

### 直接使用 Node.js

```bash
# 导出默认格式
node export-pdf.js

# 批量导出
node export-pdf.js --batch

# 查看帮助
node export-pdf.js --help
```

## ⚙️ 自定义配置

编辑 `export-pdf.js` 文件顶部的 `CONFIG` 对象：

```javascript
const CONFIG = {
    htmlPath: path.join(__dirname, 'index2.html'),  // HTML 文件路径
    outputPdf: 'KATE优势身份证_完整报告.pdf',       // 输出文件名

    format: {
        size: 'A4',           // 纸张大小: 'A4', 'Letter', 等
        margins: {
            top: '0.5cm',     // 上边距
            right: '0.5cm',   // 右边距
            bottom: '0.5cm',  // 下边距
            left: '0.5cm'     // 左边距
        },
        printBackground: true,    // 打印背景（渐变等）
        orientation: 'portrait'   // 方向: 'portrait'(纵向) 或 'landscape'(横向)
    },

    viewport: {
        width: 1200,   // 浏览器宽度
        height: 1600   // 浏览器高度
    },

    waitTime: 2000,    // 等待页面渲染时间（毫秒）
    headless: true     // 是否无头模式（不显示浏览器窗口）
};
```

## 📊 支持的纸张格式

- `A4` - 210 x 297 mm
- `A3` - 297 x 420 mm
- `Letter` - 8.5 x 11 inch
- `Legal` - 8.5 x 14 inch
- `Tabloid` - 11 x 17 inch

或者自定义尺寸：
```javascript
size: { width: 800, height: 1200 }
```

## 🎨 导出效果说明

这个工具会完美保留：
- ✅ 所有 CSS 样式
- ✅ 渐变背景
- ✅ 阴影效果
- ✅ 字体和排版
- ✅ 布局结构
- ✅ 图片和图形

## 🐛 常见问题

### Q: 为什么首次运行很慢？
A: 首次运行需要下载 Chromium 浏览器，后续运行会很快。

### Q: PDF 中字体不对？
A: 确保网络连接正常，脚本会自动加载 Google 字体。

### Q: 如何调整 PDF 页数？
A: 如果内容过多自动分页，可以调整 `viewport.width` 或 `format.size`。

### Q: 页面被截断了怎么办？
A: 增加 `waitTime` 值，确保页面完全加载。

## 📂 文件结构

```
port/
├── index2.html           # HTML 源文件
├── export-pdf.js         # 导出脚本
├── package.json          # 项目配置
├── README.md             # 使用说明（本文件）
└── *.pdf                 # 导出的 PDF 文件
```

## 🔧 系统要求

- Node.js >= 14.0.0
- npm >= 6.0.0
- Windows / macOS / Linux

## 📝 许可

MIT
