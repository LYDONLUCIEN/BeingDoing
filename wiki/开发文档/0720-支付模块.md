# 支付模块配置操作手册（2026-07-20）

> 面向运营/部署。技术设计见 `tasks/payment-module-plan.md`，领域术语见 `CONTEXT.md`，关键决策见 `docs/adr/0005~0007`。

## 一、已完成的开发（无需配置即可用）

- **P1 折扣券**：admin 后台「支付管理 → 折扣券」创建/改额/作废；「邮件群发」勾选「附折扣券」自动逐人发券
- **P2a 支付宝闭环**：下单 → 扫码 → 回调自动发码 → 我的订单 / admin 订单与退款（代码已上线，**密钥未配置时下单接口返回 503，不影响其他功能**）

## 二、支付宝开放平台配置（你需要操作的）

### 1. 不用填的（别被页面迷惑）

| 配置项 | 说明 | 操作 |
|---|---|---|
| 应用网关 | 接收平台消息用（生活号等），**与支付通知无关**，不是我们网站的网关 | ❌ 不填 |
| 授权回调地址 | 仅「支付宝登录/OAuth」用，我们不用支付宝登录 | ❌ 不填 |
| openid 配置管理 | 会员/营销类接口用 | ❌ 不管 |
| 接口内容加密（AES） | 仅「获取会员手机号」等能力强制，支付接口不受影响 | 随意 |
| 支付宝网关地址 | 不是填写项，代码已默认 `https://openapi.alipay.com/gateway.do` | 不用动 |

### 2. 必须确认的

| 配置项 | 操作 |
|---|---|
| 产品开通 | 应用 → 产品列表 → 开通「**当面付**」（沙箱应用默认已开） |
| 接口加签方式 | 选「密钥」；保存好**应用私钥**（本地生成的 pem）；在平台上查看/下载**支付宝公钥**（注意：不是应用公钥） |
| 服务器 IP 白名单 | 若设置了，**必须包含服务器公网出口 IP**，否则下单/退款接口被拦截 |

### 3. 支付异步通知的地址（最容易误解）

支付结果通知**不走「应用网关」**，而是每笔下单请求里用 `notify_url` 参数逐笔指定（代码已实现）。你只要保证 `.env` 的 `ALIPAY_NOTIFY_URL` 公网可达 + HTTPS：

```
ALIPAY_NOTIFY_URL=https://你的域名/api/v1/payment/notify/alipay
```

本地调试回调：用内网穿透（natapp/cpolar 等）把本地 8000 暴露为公网 https 临时域名。

## 三、.env 配置清单（项目根目录）

```bash
# ===== 已填好，勿动 =====
ACTIVATION_CODE_PRICE=9900        # 激活码价格（分）
ACTIVATION_CODE_TTL_DAYS=180      # 有效期天数
DEFAULT_COUPON_AMOUNT=5000        # 邮件发券默认面额（分）
ORDER_TIMEOUT_MINUTES=30          # 订单超时关单
MEMBERSHIP_ENABLED=false          # 会员开关（未开放）
MEMBERSHIP_MONTHLY_PRICE=1500
MEMBERSHIP_LIFETIME_PRICE=29900
MEMBER_DISCOUNT_PERCENT=85

# ===== 需要你填（支付宝）=====
ALIPAY_APP_ID=
ALIPAY_PRIVATE_KEY_PATH=src/backend/certs/alipay_app_private_key.pem
ALIPAY_PUBLIC_KEY_PATH=src/backend/certs/alipay_public_key.pem
ALIPAY_NOTIFY_URL=https://你的域名/api/v1/payment/notify/alipay
ALIPAY_GATEWAY=https://openapi.alipay.com/gateway.do   # 沙箱改为 https://openapi-sandbox.dl.alipaydev.com/gateway.do

# ===== 微信（P2b 再做，先留空）=====
WECHAT_MCH_ID=
WECHAT_APP_ID=
WECHAT_API_V3_KEY=
WECHAT_CERT_SERIAL_NO=
WECHAT_PRIVATE_KEY_PATH=
WECHAT_NOTIFY_URL=
```

密钥文件放 `src/backend/certs/`（已加入 .gitignore，**绝不提交 git**）。

## 四、沙箱联调流程（建议先走）

1. 开放平台 → 沙箱环境：拿沙箱 APPID、沙箱密钥、下载「沙箱版支付宝」买家 APP
2. `.env` 填沙箱三件套 + 沙箱网关 + 穿透域名回调地址
3. `./start.sh restart backend`
4. 网站下单 → 沙箱买家 APP 扫码付款 → 回调自动发码 → 个人空间「我的订单」查看
5. admin「支付管理 → 订单管理」测试退款（码未被使用时）

## 五、验收清单

- [ ] 下单返回二维码
- [ ] 扫码支付后 2 秒内订单变 granted 且展示激活码
- [ ] 邮箱收到激活码备份邮件
- [ ] 重复回调不重复发码（看日志）
- [ ] 超时 30 分钟未支付自动关单、券自动释放
- [ ] admin 退款：码已使用被拒；未使用退款成功且码作废
