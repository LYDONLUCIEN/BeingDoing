# 0919 UI 迁移决策记录（grill-me 拷问结果）

> 日期：2026-09-19
> 目标设计稿：`wiki/开发文档/0919/openlife-journey (21).html`（唯一视觉准绳）
> 参考文档：`0919-newui-UI设计范式与实现标准.md`、`0919/OpenLife-UI-Design-System.md`（与 HTML 冲突时以 HTML 为准）
> 工作分支：`newui`

## 关键事实核查

- HTML 中**不存在 blur(6px)**；激活页背景模糊默认 10px，纸卡 12+10=22px，全站值域 4–28px。
- `#activate-values` 是 URL hash 而非页面/元素 id，对应现有 `/explore/activate`。
- `/explore/intro` 在 main 分支**确认存在**：5 步流程引导（STEPS×5）+ 分流逻辑（已登录且有已绑定 full 码 → 直达 activate）。
- HTML 默认气泡配色（RECOMMENDED_APPEARANCE）：**AI 深墨 #202832 底白字 / 用户纸白底深字**，不对称切角（AI `4px 19px 19px 19px`，用户反向），无左色条。
- 站内信在 HTML 中 = 右下固定 pill 悬浮球 + 右侧 456px 抽屉（blur 28px，双 tab 站内信/反馈）+ 个人空间嵌入面板，无独立路由页。

## 已拍板决策（10 条）

| # | 议题 | 决策 |
|---|---|---|
| 1 | 毛玻璃数值 | 激活码页/intro/关于我们/社区用 **6px**；其余页面按 HTML 实际值（10–28px） |
| 2 | /explore/intro | **保留页面与内容，只换背景**为半透明纸层+6px 毛玻璃；已核实=用户记忆中的「找个安静空间」引导页（premise1 文案+5 阶段） |
| 2b | intro 分流 | 「一闪而过」根因=完整码用户刻意直达 activate；改为**仅首次看**（localStorage 记录，之后直达），原 full 码分流逻辑保留但让位于首次展示 |
| 2c | 忘记密码 | HTML 无对应 UI（零命中），**保留现有 AuthModal 忘记密码不动**（功能并集原则） |
| 3 | 开始探索跳转 | intro 保留（见 #2），activate 页按 HTML 激活页重画（6px 纸层） |
| 4 | PurchaseModal | **完全不动**（支付模块红线） |
| 5 | 社区/关于我们 | 背景换半透明纸层+6px 毛玻璃（内容不变） |
| 6 | 站内信 | 入口改 **HTML 右下固定 pill + 右侧抽屉**；功能**全面对齐 HTML**（详情视图、类型 badge、动作按钮），保留真实后端数据/接口 |
| 7 | 个人空间 | **HTML 有的面板才重画**：进度/指南/帮助/回收站/激活码/团队分析/设置；「报告解读」页暂不动；侧栏 8 项入口全保留 |
| 8 | Chat 外观弹层 | **用户可见**（学 HTML #appearance-popover），localStorage 持久化；**先独立开关矩阵后固化**（决策后删落选代码） |
| 9 | Chat 变体维度 | 独立开关：气泡间距（紧凑=HTML 28px / 宽松=现有）× 侧栏装饰（带图=HTML 植物贴纸 / 不带图）× 新建对话样式（虚线浅底=HTML / 深墨实心=现有） |
| 10 | 气泡配色 | **按 HTML 默认**：AI 深墨、用户纸白、不对称切角、无左色条（实施时发现 `openlife-chat-defaults.css` 已按 0919-newui §6.3 实现相反方案，已在该最终覆盖层中按 HTML 翻换；墨底气泡内 markdown 文字用 `!important` 提亮压过 markdown.css） |
| 11 | Rumination | **也按 HTML 重画**（三栏 selection-panel 布局；用户明确拍板，覆盖文档 6.4 保护线，状态机逻辑仍保留） |
| 12 | 节奏 | **先简后繁**：背景页 → 站内信 → 个人空间 → Chat；**全部做完统一验收**（lint+build+截图） |
| 13 | Admin 管理台 | **也按新 UI 规范换肤**（纸层/毛玻璃/深墨按钮），**功能零改动** |
| 14 | 总原则 | **功能并集**：以现有实际服务为基准，只能多不能少；HTML 缺失的内容绝不擅自删除；拿不准就问 |

## 不可破坏边界（沿用 0919-newui 文档）

- 不改 API 参数、鉴权、数据结构与业务状态机；不伪造后端能力。
- 页面级作用域样式（`.ol-*`/稳定前缀），禁止无作用域全局选择器误伤首页与支付。
- `backdrop-filter` 必带 `-webkit-` 前缀；`color-mix` 先写静态回退；视口高度用 `.chat-shell-h`。
- 图片素材解码放 `public/assets/` 并 Git 跟踪，不用 base64。
- 每轮验收：`git diff --check`、`npm run lint`、`npm run build`。

## 反馈修复轮（2026-09-20，用户验收后）

| # | 问题 | 处理 |
|---|---|---|
| F1 | 当前进度旅程轨道不像 HTML | 当前节点改 HTML 样式：白底+2px 阶段色描边+发光；完成=阶段色+✓角标；未开始=纯灰+锁 |
| F1b | 个人信息卡缺进度条/编辑不是独立页 | 新增 `computeSurveyCompletion`（18 字段）；卡片加 HTML 渐变进度条（#6c9ddd→#70b9a1）；新增独立页 `/dashboard/profile/edit`（进度条+完整表单，保存接口与 settings 同源） |
| F2 | 修改密码「闪退」 | 当前构建实测全链路正常（改密→撤会话→重登验证通过）；成功路径的 1.2s 后登出跳首页为既有设计，已向用户说明 |
| F3 | 关于我们/协议排版 | 关于我们加「← 返回首页」；LegalDocModal 正文内嵌白纸阅读面（.legal-doc-pane）+强制左对齐 |
| F4 | 报告预览页未迁移 | 根容器换 PaperVeilLayers 纸层背景、紫色按钮/图标全转深墨#222b35；修了一个层级 bug（fixed 纸层盖住 static 内容，加 `.ol-report-shell > *:not(.ol-paper-bg){z-index:2}`） |
| F5 | chat「另一种设计」在哪 | 解答：气泡配色已全局切换（非开关）；齿轮弹层只管间距/贴纸/按钮三维；rumination 已换紫色皮肤但无布局开关（布局受状态机保护） |

## 拷问轮（2026-09-22，rumination 细节对齐，5 决策全部拍板）

| # | 议题 | 决策 |
|---|---|---|
| 15 | 05沉淀标题对齐 | **19px + 保留副标题**（13px muted 居中，✦ 保留；前4阶段无副标题，HTML 设计稿有——信息并集） |
| 16 | 毛玻璃/紫色背景 | **按 HTML 还原**：对话卡白度 0.55/左栏 0.45、blur 12；补 HTML `.rumination-flow` 四色淡流光（柔和淡彩 blob，非 accent 纯色）+ 紫色 radial 主导 |
| 17 | 矩阵默认配色 | **保持 duo**（热爱珊瑚×优势雾蓝）——"看不出紫"根因之一但用户拍板不改；紫靠背景/按钮/左栏/结论卡体现；violet/multi 开关保留 |
| 18 | 左栏组合块 | **中性白底细边 chips + 紫编号 + 状态 meta 行**（238px、min-height 74px、chips 全量直出不收 "+N"） |
| 19 | guided 默认 | **guided 三栏设为默认**（persist v1→v2 迁移把存量 classic 一并升级，面板可切回） |

> 同轮直接修复（未问）：完成并继续统一 `.bd-btn-black`；新建组合蓝→紫虚线（sidebar/TopComboBar/锁屏 CTA）；矩阵卡 3D 质感=结论卡同款（对角渐变+顶部高光+分层阴影）。
> 详见 `../claude-completed-0922-rumination-guided左栏与视觉对齐.md`。

## 反馈修复轮（2026-09-23，guided 结构对齐，用户验收 0922 后）

| # | 问题 | 根因 → 处理 |
|---|---|---|
| F6 | 新建组合应属左侧、层次太多 | `.v4-outer-shell` 是**整页玻璃罩**（白 0.66+blur22，违反"禁止整页大磨砂玻璃"）→ guided 分支不再渲染该壳：V4ComboSidebar 直接挂根级全高左栏（同前四阶段侧栏/HTML .sidebar，border-r+白 0.45+无 blur）；classic 保留壳（mist 等皮肤依赖） |
| F7 | 选择器壳不透明、内层 div 多余 | guided 不渲染 `.v4-inner-pane`（白 0.74 玻璃）；V4MatrixLeftPanel 两个面板 div（白 0.46）改纯布局容器（透明无边框）——选择卡/结论卡自带表面直接落背景，层级从 4 层减到 1 层 |
| F8 | 整体颜色还是看不见 | 除 F6 外第二元凶：`rumination-journey-ribbon` 照片纹理 0.55 不透明度整页铺盖 → 删除（HTML rumination 背景只有流光+贴纸，无 ribbon） |
| F9 | 对话卡应像前四阶段是画面主体 | outer-shell 删除后对话卡（flex-1）即内容列主体，流光背景直接可见 |
| F10 | 左栏 chips 全白不好看 | 白底中性 chips → duo 色呼应矩阵：热爱珊瑚浅底（#fdeef1/#c8455a）× 优势雾蓝浅底（#eef3fd/#3569d4），色值与矩阵选中态同源 |
| — | 附带 | workbench 宽度覆盖 `calc(100%-24px)` 排除 guided；小高度媒体查询 hero h1 24px 覆盖移除（保持 19px 对齐）；workbenchRef 双分支挂载+effect deps 补 ruminationLayout |

## 实施中确认的开放点

- ~~站内信详情视图「动作按钮」依赖后端通知是否带跳转字段~~ → **已确认**：后端 `Notification` 只有 `related_feedback_id`，无跳转字段，按「不伪造」原则详情视图**不加动作按钮**（2026-09-19 实施时确认）。
- 站内信类型 badge 映射已覆盖后端全部 11 种 type（含 admin 专属的 feedback_new/feedback_overdue/llm_balance_low）；旧浮窗的拖拽书签/隐藏小×交互随形态切换移除（HTML 无此设计）。
- ~~Chat 开关矩阵的 localStorage key 与旧外观存储的迁移适配~~ → 已实现：`openlife-chat-appearance`（zustand persist），默认=现状（宽松/不带图/深墨实心），用户切到 HTML 方向即时生效；`ChatUiPreview` 同步接入同一 store，可用 `?ui_preview=1&preview_state=conversation`（dev）做 A/B 截图对比。
- Rumination 重画时组合矩阵/解锁/状态机逻辑逐条对账保留。
