# 寻路·LifeAsk (lifeask) — 领域术语

> 面向产品、运营与开发共用的语言。不含实现细节。

## 品牌

**品牌名 (Brand Name)**  
中文语境书写为「寻路·LifeAsk」（间隔号连接），英文/域名/代码语境书写为 `LifeAsk` / `lifeask`（小写用于域名、文件名、配置值）。生产域名为 `lifeask.beyondego.me`。旧称「寻路 (xunlu)」已废弃，新文案不得再使用。
_Avoid_: 寻路LifeAsk（无间隔号连写）、xunlu、Xunlu、XUNLU

## 探索流程

**阶段 (Phase)**  
用户探索路径上的五个主阶段：`values`（价值观）、`strengths`（优势）、`interests`（兴趣）、`purpose`（使命）、`rumination`（沉淀）。

**沉淀子步 (Filter Step)**
沉淀阶段内的筛选步骤（编号 1–7 + 3b），用于表格筛选、假设生成、方向点选等。每子步可有独立的引导语与对话追加提示词。

**组合矩阵 (Combination Matrix)**
`filter_step=3`（沉淀 v3）时，用户前面探索出的热爱（最多3个）与优势（最多5个）构成 3×5=15 个格子。每个格子是「1 热爱 × 1 优势」，有独立对话与假设结论；combo_id 格式为 `passion_idx + strength_idx`（如 `"02"` = 热爱1×优势3）。
_Avoid_: 用「组合矩阵」指代 v4 的自由多优势会话

**组合会话 (Combo Session)**
沉淀 v4 中一次可讨论的方向单元：恰好 1 个热爱 + 1..N 个优势。用户在选择器里点选后点「开始探索」创建；与 v3 的单格组合矩阵不同。
_Avoid_: Combo、矩阵格子、Combination Matrix

**选点草稿 (Selection Draft)**
尚未提交的热爱/优势选点状态。默认进入时无任何选中；点「新建组合」也回到此态（清空 active，解锁胶囊供重选）。草稿态下右侧 AI 输入区模糊锁定，不可发送。
_Avoid_: 空组合会话、未创建的 combo

**开始探索 (Start Explore)**
把当前 **选点草稿** 提交为 **组合会话** 并开场的动作。成功后右栏 AI 解锁可输入；此前右栏保持锁定。已存在的会话被选中时，该按钮隐藏或禁用（组合已固定）。
_Avoid_: 新建组合（那只是进入草稿）

**选择器胶囊 (Selector Capsule)**
v4 左栏选择控件：块状网格（热爱一行三列、优势一行五列），视觉复用 v3 橙热爱 / 绿优势（玻璃底、选中渐变、对勾）。热爱单选；优势多选。处于已选 **组合会话** 时只读回显并锁定；要改组合须点「新建组合」回到 **选点草稿**。
_Avoid_: 紫玻璃大图标卡（HTML mock 原样紫边）；竖排长条列表；在已开聊会话上原地改热爱/优势

**维度图标 (Dimension Glyph)**
热爱/优势胶囊上的语义化 emoji（或等价符号）。每个维度一项，按语义填充；UI 必须预留该字段，未提供时用类型默认值（热爱 ♥ / 优势 ✦）。本轮只预留组件/类型字段，不接语义字典、不改 `matrix_snapshot` schema。
_Avoid_: 写死的装饰图标、与文案无关的占位图

**v4 沉淀页壳 (V4 Rumination Shell)**
整页**一个**大容器；背景与内部分区使用 v3 step3 同款毛玻璃（无紫色描边/紫阴影）。大容器内从上到下：顶栏居中「05 沉淀」+ 描述、右上角「完成并继续」；下一行组合 tabs（可水平滑动）+「新建组合」；再下为左选择器/结论与右 AI 对话两个内部分区（不是两个独立外层壳）。选择器为块状网格：热爱一行三列、优势一行五列。强调色：热爱橙 / 优势绿；tabs 与「新建组合」用 v3 青系或中性描边；**仅「开始探索」主 CTA 保留 HTML 紫渐变**。
_Avoid_: HTML mock 右侧 combo-tabs；紫边双独立 shell；选择器竖排长条；tabs 用 flex 均分导致挤成两格无法横滑；整页紫强调

**v4 结论卡区 (V4 Conclusion Panel)**
左栏下部只展示**当前**组合会话的结论卡（一张，不列出全部卡；切换靠组合 tabs）。7-25 版起卡面极简 = **假设（可编辑）+ 确认/跳过**，motivation/work_purposes/passion_mark/timing_mark/balance 字段全部仅后台记录。用户「跳过」**不清空卡**：内容保留 + 删除线 + 灰色 + 「已跳过」标记，且**可逆**（灰色卡上点「确认」恢复 concluded）；只有 concluded 卡进终选。
_Avoid_: HTML「已保存 N 张」总列表；与 tabs 重复的多卡导航；跳过即清空卡；前端渲染 balance 字段

**平衡点 (Balance Point)**
沉淀 v4（7-25 版）中 AI 对某组合会话假设方向的判定：该方向既能承载内心渴望（价值观一致、忍不住想做），又能在当下付诸实践（投入可接受、有预期回报）。判定结果存为结论卡的**前端隐藏字段** `balance_found`（true/false/**null=未评估**）+ `balance_fail_reason`，随对话可被 AI 反复翻转；hypothesis 一旦变更（新 chips 选定/对话修改/手动编辑卡），后端立即置 null 触发**再评估**（AI 用已有证据重核四要素，仅对证据被推翻的维度补问一个问题）。AI 判为难平衡时输出「难平衡话术」（要点式，措辞自由：坦诚表达顾虑 + 明确不建议 + 给替代路径 + 尊重继续意愿），不强制终止对话；用户坚持不匹配也并入此机制。除非用户手动「跳过」，卡永远可选可改；终选（1–3 个）卡片统一紫色主题（数字标签/选中态/优势 tags），不渲染三态徽章；唯一差异 = 不推荐（`balance_found=false`）卡片底部琥珀 ⚠ 警告条（`balance_fail_reason`）。假设生成须先问用户倾向（独立经营 vs 公司岗位）：倾向明确 → 2 条同方向不同切面候选；模糊 → 独立+公司各一；恒 2 条，与优势数量无关；候选经 **chips 选择器**（复用 v3 `[STEP3_HYP_JSON]` 协议，三选项 [A][B][✏️自己写]，点击=作为用户消息发送，不直写结论卡）让用户确定性选择。
_Avoid_: 把平衡点做成强制跳过/终止对话的闸门；不问倾向直接给双方向各一条；chips 点击直写结论卡；hypothesis 用 dict 分优势形态（已废弃，恒为字符串）

**组合假设状态 (Combo Hypothesis State)**
每个组合有三种状态：`empty`（未填写）、`confirmed`（已确认假设）、`skipped`（已跳过，保留文字但灰显标记）。跳过分两种：纯空跳过和有字跳过（后者值得在全部提交时追问）。适用于 v3 组合矩阵格子。

**深度讨论 (Deep Discussion / filter_step=3b)**
`filter_step=3b` 回归表格+全局对话模式，表格只含已确认假设的组合（列：热爱、优势、假设），全部解锁，可选中行提问。是当前 step3 的等价替换，后续衔接 step4+ 逻辑不变。

## 支付与商业化

**试用激活码 (Trial Code)**  
注册即送、自动绑定到用户的免费码（每人仅一个，老用户无码时登录补发）。不限有效期，但**仅限 values 阶段问答且限 10 轮**（轮=用户消息条数，第 10 条正常回复、第 11 条起拦截弹购买；values 内已出结论卡要推进 strengths 时同样拦截）。LLM 档位 vip_level=1。

**完整激活码 (Full Code)**  
解锁全部五阶段+报告的码，无轮数限制，LLM 档位 vip_level=2。来源：试用码付费升级、购买套餐发新码、延期激活、管理员免费渠道。有效期**从付费升级/首次激活起算**（购买时间仅审计）。存量码（本功能上线前）一律视为完整码。
_Avoid_: 付费码、正式码

**码的双角色**  
**所属人 (Purchaser)** = 购买该码的用户（订单归属）；**激活人 (Activator)** = 实际使用该码问答的用户（claim 归属）。两者可不同（赠品码转送）。

**季度套餐 (Quarterly Package)**  
¥69：把试用码升级为完整码（或名下无试用码时发新完整码），有效期 3 个月；含 1 份报告。

**年度套餐 (Annual Package)**  
¥128（2026-07-28 由 ¥99 调整）：1 个码升级/新发 + **2 个赠品完整码**（未绑定、可转送，有效期各自从激活起算），共 3 份报告能力，有效期 1 年；含**团队分析**能力。

**延期激活 (Code Renewal)**  
对已有完整码追加有效期：2026-07-28 起**任意完整码统一 ¥20 加 3 个月，不分套餐**（旧分档：季度码 ¥23/3 个月、年度码 ¥33/1 年，已弃用）。从 max(当前到期时间, 现在) 起追加。买套餐永远是新码/升级，**不**自动延期旧码。

**报告解读咨询 (Report Consultation)**  
¥298/次：一对一线上沟通 60 分钟。前置：至少一份已完成报告。购买后填预约问卷（主题/期望、候选时间段、联系方式、备注）；状态机：待填问卷→已提交→已预约→已完成。**仅未预约前可退款**。

**报告审核 (Report Review)**  
报告生成后**阻塞式审核**：用户侧显示「审核中」，admin 确认后用户才可见。兜底：随机定 3~24 小时时限，超时未审则**自动批复**通过——后台如实记录「自动批复」，但用户侧只感知「管理员审核通过」。计时起点（2026-07-27 起）：新建报告为 **未开始**（not_started，不计时），用户**进入报告页且五阶段已完成**时才转 **待审核** 开始 3~24h 倒计时，同一时刻后台预生成报告（审核期间报告已在自动生成）。

**报告授权 (Report Authorization)**  
激活人在报告页一键授权，仅授权给该码的**所属人**（购买者），授权内容是**报告**；对话过程记录任何情况下不可分享、不可授权。

**团队分析 (Team Analysis)**  
年度套餐能力：选择多份报告（自己名下码的 + 自己订单交付且被激活人授权的）生成团队匹配度分析与团队角色投射。所属人可看到自己订单交付的所有码「被谁激活」，但看不到对话内容。

**会员 (Membership)**  
用户账户级的长期服务订阅（包月 ¥15 / 永久 ¥299 / 买码 85 折），与套餐是**并存的独立产品线**。数据库与接口保留（ADR-0006/0007），**本期不开发不展示**，后续上线首页。会员折扣对套餐的适用规则届时再定。
_Avoid_: 用「会员」指代任何套餐或码

**会员折扣 (Member Discount)**  
会员购买时自动生效的价格减免（8.5 折）。与**折扣券**是两套独立机制，可叠加：先算会员价，再减券面额，实付下限 0 元。（会员上线前不适用）

**支付订单 (Payment Order)**  
一次购买行为的单据：购买者、商品（季度/年度套餐、延期、咨询）、渠道（支付宝/微信）、金额（原价/抵扣/实付，以分存储）、状态。状态机：`pending → paid → granted`；`pending → closed`/`cancelled`；`granted → refunding → refunded`。
_Avoid_: 交易、流水、bill

**折扣券 (Coupon)**  
通用券码（不绑定用户）：一个码 + 固定金额（无门槛）+ 永久有效，**核销一次即作废**。下单时输入券码抵扣；下单即锁定、订单关闭/取消自动释放。
_Avoid_: 优惠券、代金券、promo code

**券池 (Coupon Pool)**  
全部未使用折扣券的集合。邮件发券按创建时间 FIFO 取券；池空时按系统默认金额（`DEFAULT_COUPON_AMOUNT`）自动创建补足。

**退款规则**  
码类商品（套餐/延期）：仅**未使用**（未被任何用户 claim）的激活码可退款，退款成功即作废码。咨询：仅未预约前可退。退款由 admin 在后台发起（官方退款 API），用户无自助入口。

**发码自取**  
购买套餐交付的**赠品码**：支付成功页展示 + 邮件备份，用户自行分发/激活，码不绑定购买者可转送。对比：试用码注册即自动绑定，不走此路径。

## 提示词分类

**阶段引导语**  
进入或离开某阶段时展示给用户的弹窗文案（intro / outro），不直接作为 LLM system prompt。

**主对话提示词**  
该阶段 LLM 对话使用的 system prompt 主体，按阶段分支。

**子步对话追加 (Chat Addon)**  
沉淀子步 1–7 各自追加到主对话 system prompt 的片段。

**子步引导语 (Step Opening)**  
进入沉淀某子步时的开场白。分两种模式：**固定文案**（直接展示）或 **LLM 生成**（由 system + user 模板驱动）。

**兜底 (Fallback)**  
主路径失败或未命中时的备用文案，例如首轮开场失败、零结果闸门、探索阶段 init 兜底等。

**运行时追加**  
对话过程中动态拼接到 system prompt 的内容（如状态协议块、使命进度、结论卡待确认摘要），非静态配置文件中的完整段落。

**Canonical 配置**  
Git 中的 YAML 与 Python 常量，生产环境默认使用的提示词来源。

**Prompt Lab 覆盖**  
仅在调试沙箱激活码下生效的提示词改写（当前限于主对话模板与 extra_goal_hint），不改变 canonical 配置。

**Effective 提示词**  
某次沙箱对话实际生效的拼装结果：canonical Layer Stack + 可选 Prompt Lab 覆盖 + 运行时注入。

**注入点 (Injection Point)**  
运行时内容拼接到 system prompt 的位置；在提示词总览中以分层堆叠视图标注顺序与触发条件。

**变量占位符 / 示例上下文**  
模板中的 `{{ variable }}`；悬停可查看固定 mock 示例，不展示真实用户数据。

## 提示词总览（Prompt Lab 扩展）

合并进 Prompt Lab，不单独开页。按五阶段浏览；沉淀阶段内按子步 1–7 展开。v1 不含 LangGraph 节点提示词。双语项（如 step_copy、chat_addon）通过页顶 **中/英切换** 展示。

## 调试与隔离

**调试沙箱 (Sandbox Fork)**  
从正式激活码 fork 出的隔离环境（`SBX*` 激活码），用于测试，不影响生产用户。

**Savepoint**  
沙箱会话在某一刻的状态快照；载入后可从该点继续手动测试，不修改 canonical 提示词文件。沉淀/子步调试时优先使用。

**试跑**  
在沙箱中验证提示词效果的两条路径：**从 Savepoint 续测**（默认/主路径）与 **Fork 从头试跑**（整阶段流程）。

## Relationships

- 一个 **组合会话** 绑定恰好 1 个热爱与 1..N 个优势
- 每个热爱/优势可带一个 **维度图标**
- **组合矩阵**（v3）的一格 ≠ 一个 **组合会话**（v4）
- **选择器胶囊** 是创建 **组合会话** 前的选点 UI，不是矩阵格子本身
- **v4 沉淀页壳** 包住顶栏、tabs、左选择器与右 AI；组合切换 / 新建组合属于大容器顶栏下一行，不属于右栏
- 「新建组合」进入 **选点草稿**；「开始探索」把草稿变成 **组合会话** 并解锁右栏
- **选点草稿** 期间右栏 AI 输入模糊锁定；已存在的 **组合会话** 被选中时右栏可输入
- **v4 结论卡区** 只跟当前会话走；换会话靠大容器内组合 tabs
- 已开聊的 **组合会话**：选择器只读锁定；改方向必须经「新建组合」回草稿
- 大容器右上角「完成并继续」与 v3 顶栏一致
- 一个 **支付订单** 至多使用一张 **折扣券**；核销成功即作废该券
- **季度套餐** 订单交付 = 升级 1 个 **试用激活码**（无试用码时发新 **完整激活码**）；**年度套餐** 订单交付 = 1 升级/新码 + 2 个赠品 **完整激活码**（发码自取）
- **延期激活** 订单只追加有效期，不交付码；**报告解读咨询** 订单交付预约问卷，不交付码
- 每个 **完整激活码** 有所属人（购买者）与激活人（使用者）两个角色；激活人可把**报告**一键授权给所属人，对话记录不可授权
- **报告** 生成后须经 **报告审核**（人工确认或超时 **自动批复**）才对用户可见
- **会员** 与套餐是并存的两条产品线（会员本期不上线）；**退款** 只发生在 **支付订单** 上，码类商品仅未使用码可退，咨询仅未预约可退

## Example dialogue

> **Dev:** "用户在 v4 里选了『帮助与影响』加三个优势，这还叫组合矩阵吗？"
> **Domain expert:** "不叫。那是一个 **组合会话**。**组合矩阵** 只指 v3 里 1×1 的格子。"

> **Dev:** "胶囊上的 ♥ 是装饰吗？"
> **Domain expert:** "不是，那是 **维度图标**，要按该热爱/优势的语义填；暂时没有语义结果时才用默认符号。"

> **Dev:** "组合 tabs 还跟 HTML 一样放在 AI 对话框上面吗？"
> **Domain expert:** "不。tabs 和「新建组合」在大容器顶栏下一行；「05 沉淀」居中，右上角是「完成并继续」。"

> **Dev:** "一进页就能在右边打字吗？"
> **Domain expert:** "不能。默认是 **选点草稿**，右边模糊锁定；点「开始探索」创建会话后才解锁。"

> **Dev:** "结论区要列出所有已保存卡吗？"
> **Domain expert:** "不要。**v4 结论卡区** 只显示当前会话那一张；其它会话用 tabs 切。"

> **Dev:** "切回已有组合后还能改热爱吗？"
> **Domain expert:** "不能。选择器只读回显；要改就点「新建组合」进草稿再「开始探索」。"

> **Dev:** "切回已有组合后还能改热爱吗？"
> **Domain expert:** "不能。选择器只读回显；要改就点「新建组合」进草稿再「开始探索」。"

> **Dev:** "会员还需要买激活码吗？"
> **Domain expert:** "**会员** 体系本期不上线，先不讨论。现在用户注册即送 **试用激活码**，限 values 10 轮；要买的是 **季度/年度套餐**，付费后试用码变 **完整激活码**。"

> **Dev:** "给 100 个人群发邮件附折扣券，是同一个码吗？"
> **Domain expert:** "不是。**折扣券** 一次核销即作废，所以从 **券池** 里给每人分一张独立码；池子空了系统自动按默认金额造新的。"

> **Dev:** "小明买了年度套餐，把赠品码送给小红，小红的报告和对话小明都能看到吗？"
> **Domain expert:** "报告可以——但得小红在报告页一键 **授权** 给所属人（小明）。对话过程记录谁都看不了，不能分享。"

> **Dev:** "报告生成完用户就能看吗？"
> **Domain expert:** "不能，先 **报告审核**：admin 确认才可见；但有个 3~24 小时的随机兜底，超时 **自动批复** 通过，用户只知道『管理员审核通过』。"

> **Dev:** "用户激活码已经用了一半，能退款吗？"
> **Domain expert:** "不能。**退款规则** 是仅未使用的码可退；咨询则是未预约前可退。"

## Flagged ambiguities

- 「矩阵 / matrix」曾同时指 v3 的 3×5 格子与 v4 选择区 — 已拆成 **组合矩阵** vs **组合会话** + **选择器胶囊**。
- HTML mock 的紫大卡片 ≠ 产品要的视觉；视觉以 v3 橙/绿胶囊与毛玻璃为准。
- HTML mock 把 combo-tabs 放在右栏 — 已否决；见 **v4 沉淀页壳**（tabs 在大容器顶栏下一行）。
- 「新建组合」≠「开始探索」：前者进草稿，后者才创建并解锁聊天。
- HTML「已保存 N 张」列表 — 已否决；见 **v4 结论卡区**。
- 已开聊会话上原地改热爱/优势 — 已否决；组合固定，改方向走新建。
- 整页紫强调 — 已否决；仅「开始探索」保留紫 CTA，其余跟 v3。
- 大容器右上角需要「完成并继续」（与 v3 顶栏一致）；不再「顶栏仅标题」。
- 「会员买码」曾被理解为会员免码或码延期 — 已否决：**会员** 与 **激活码** 是两条产品线，会员仅享折扣与高级模型固化。（会员本期不上线）
- 「折扣券」曾被讨论为绑定用户/百分比/满减 — 已否决：通用码、固定金额、无门槛、永久有效、核销一次即作废。
- 「发码自取」曾被讨论为自动绑定购买账户 — 已否决：套餐赠品码**发码自取**可转送；但**试用激活码**相反，注册即自动绑定。
- 「连续包月」曾被讨论为到期提醒手动续费 — 已否决：按真实代扣（委托代扣/周期扣款）设计，但会员功能本期不开放。
- 「全程激活码 ¥99/180 天」单一 SKU — 已下架：商品目录改为 **季度套餐/年度套餐/延期激活/报告解读咨询**（2026-07-20）。
- 「再买套餐给原码延期」— 已否决：买套餐永远是新码/升级试用码；旧码续命走 **延期激活**（2026-07-28 起统一 ¥20/3 个月）。
- 「团队分析共享对话记录」— 已否决：只共享**报告**（且需激活人一键授权给所属人），对话记录不可分享。
