# 寻路·OpenLife (openlife) — 领域术语

> 面向产品、运营与开发共用的语言。不含实现细节。

## 品牌

**品牌名 (Brand Name)**  
中文语境书写为「寻路·OpenLife」（间隔号连接），英文/域名/代码语境书写为 `OpenLife` / `openlife`（小写用于域名、文件名、配置值）。生产域名为 `openlife.beyondego.me`。旧称「寻路 (xunlu)」已废弃，新文案不得再使用。
_Avoid_: 寻路OpenLife（无间隔号连写）、xunlu、Xunlu、XUNLU

**落款签名 (Report Signature)**  
PDF 报告末尾的引导师手写签名图。每位引导师一张签名图（当前 3 张：`signature_1/2/3.png`），报告首次生成 PDF 时随机分配一位，并持久化到 report 的 `record.json` 的 `report_signature` 字段；同一报告之后任何次再生成必须使用同一签名，不得重新随机。
_Avoid_: 每次生成重新随机签名；把签名做成用户可选项

## 探索流程

**阶段 (Phase)**  
用户探索路径上的五个主阶段：`values`（价值观）、`strengths`（优势）、`interests`（兴趣）、`purpose`（使命）、`rumination`（沉淀）。

**沉淀子步 (Filter Step)**
沉淀阶段内的筛选步骤（编号 1–7 + 3b），用于表格筛选、假设生成、方向点选等。每子步可有独立的引导语与对话追加提示词。

**组合矩阵 (Combination Matrix)**
`filter_step=3`（沉淀 v3）时，用户前面探索出的热爱（最多3个）与优势（最多5个）构成 3×5=15 个格子。每个格子是「1 热爱 × 1 优势」，有独立对话与假设结论；combo_id 格式为 `passion_idx + strength_idx`（如 `"02"` = 热爱1×优势3）。
_Avoid_: 用「组合矩阵」指代 v4 的自由多优势会话

**组合会话 (Combo Session)**
沉淀 v4 中一次可讨论的方向单元：恰好 1 个热爱 + 1..N 个优势。用户在选择器里点选后点「开始探索」创建；与 v3 的单格组合矩阵不同。
_Avoid_: Combo、矩阵格子、Combination Matrix

**选点草稿 (Selection Draft)**
尚未提交的热爱/优势选点状态。默认进入时无任何选中；点「新建组合」也回到此态（清空 active，解锁胶囊供重选）。草稿态下右侧 AI 输入区模糊锁定，不可发送。
_Avoid_: 空组合会话、未创建的 combo

**开始探索 (Start Explore)**
把当前 **选点草稿** 提交为 **组合会话** 并开场的动作。成功后右栏 AI 解锁可输入；此前右栏保持锁定。已存在的会话被选中时，该按钮隐藏或禁用（组合已固定）。
_Avoid_: 新建组合（那只是进入草稿）

**选择器胶囊 (Selector Capsule)**
v4 左栏选择控件：块状网格（热爱一行三列、优势一行五列），视觉复用 v3 橙热爱 / 绿优势（玻璃底、选中渐变、对勾）。热爱单选；优势多选。处于已选 **组合会话** 时只读回显并锁定；要改组合须点「新建组合」回到 **选点草稿**。
_Avoid_: 紫玻璃大图标卡（HTML mock 原样紫边）；竖排长条列表；在已开聊会话上原地改热爱/优势

**维度图标 (Dimension Glyph)**
热爱/优势胶囊上的语义化 emoji（或等价符号）。每个维度一项，按语义填充；UI 必须预留该字段，未提供时用类型默认值（热爱 ♥ / 优势 ✦）。本轮只预留组件/类型字段，不接语义字典、不改 `matrix_snapshot` schema。
_Avoid_: 写死的装饰图标、与文案无关的占位图

**v4 沉淀页壳 (V4 Rumination Shell)**
整页**一个**大容器；背景与内部分区使用 v3 step3 同款毛玻璃（无紫色描边/紫阴影）。大容器内从上到下：顶栏居中「05 沉淀」+ 描述、右上角「完成并继续」；下一行组合 tabs（可水平滑动）+「新建组合」；再下为左选择器/结论与右 AI 对话两个内部分区（不是两个独立外层壳）。选择器为块状网格：热爱一行三列、优势一行五列。强调色：热爱橙 / 优势绿；tabs 与「新建组合」用 v3 青系或中性描边；**仅「开始探索」主 CTA 保留 HTML 紫渐变**。
_Avoid_: HTML mock 右侧 combo-tabs；紫边双独立 shell；选择器竖排长条；tabs 用 flex 均分导致挤成两格无法横滑；整页紫强调

**v4 结论卡区 (V4 Conclusion Panel)**
左栏下部只展示**当前**组合会话的结论卡（一张，不列出全部卡；切换靠组合 tabs）。2026-08-06 起（ADR-0015）卡面 = **仅 hypothesis 一个字段，完全由用户手写/修改**——主对话 AI 不再输出/预填结论卡，只在收尾时用话术引导用户填入左侧卡片；motivation/work_purposes/passion_mark/timing_mark 字段废弃不再记录，`balance_found`/`balance_fail_reason` 为后台判定结果的前端隐藏字段。卡片状态机：**草稿**（可编辑，[确认][跳过]，确认要求 hypothesis 非空）→ **分析中**（卡与对话输入一并锁定，显示「正在分析中」；用户可切换/新建其他组合）→ **已判定**（推荐=绿灯 / 不推荐=琥珀灯+理由；[重新编辑][再聊聊]；仅已判定卡进终选）。「跳过」语义不变：**不清空卡**、删除线+灰色+「已跳过」标记、**可逆**。
_Avoid_: HTML「已保存 N 张」总列表；与 tabs 重复的多卡导航；跳过即清空卡；AI 自动填卡/预填卡；分析中允许编辑卡或发消息；草稿态渲染 balance 字段

**平衡点 (Balance Point)**
沉淀 v4 中对某组合会话假设方向的判定：该方向既能承载内心渴望（价值观一致、忍不住想做），又能在当下付诸实践（投入可接受、有预期回报）。判定**无机械规则**：以资深职业规划咨询师角色，按原生 prompt 文档 2.3 的开放探讨（建议涵盖但不限：价值观一致 / 当下能否用现有资源启动 / 「忍不住想做」还是源于外部期待或压力 / 期待的价值回报）自由裁量。**2026-08-06 起（ADR-0015）判定者从主对话 LLM 移交独立后台判定 AI**：用户在结论卡点「确认」触发；输入 = 热爱/优势组合 + hypothesis + 该组合**完整对话**（超长时滚动摘要压缩早期）；输出 JSON `{balance_found, balance_fail_reason}` 落库。判定绑定确认时的快照——**之后任何新对话或改卡，结果即作废**（灯灭、卡回未确认态），需重新点「确认」触发重判。分析期间该组合锁卡+锁对话输入（可切换/新建其他组合）；失败自动重试 1-2 次，仍失败 → 卡上「分析失败，点击重试」，视同未完成。结果推送：确认端点本身即 SSE 流（done/failed 事件）；刷新/重进页面见 running 态自动补开订阅 SSE（可降级短轮询）；进程重启后 running → failed。「完成并继续」门槛 = **所有已确认卡判定完成且结果未作废**，否则前端提示「正在分析中，请稍后」；讨论中未确认的卡、已跳过的卡不参与检查。不推荐**不阻断**：仅琥珀灯+理由+终选卡片底部琥珀 ⚠ 警告条（`balance_fail_reason`），终选（1–3 个）卡片统一紫色主题、不渲染三态徽章的口径不变。匹配度确认保持**独立首步**（md 2.1）：专问热爱与优势是否天然匹配或有冲突感；用户坚持不匹配时先剖析原因（认知偏差/过往挫败/社会标签）并指出互补可能性，仍坚持才进宣告结束路径。宣告结束保持**话术层**（坦诚说明顾虑、给替代路径、建议探索下一组合），**后端不做强制锁死**（2026-07-28 决策）。假设硬性标准按 md 原文：角色/服务对象/具体动作/目的价值的画面感、可长期持续运营、公司岗位须真实存在。passion_mark 的「忍不住想做」vs「源于外部期待或压力」取值随字段一并废弃（「应该做」为 v3 遗留，早已废弃）。
_Avoid_: 把平衡点做成强制跳过/终止对话的闸门（话术宣告结束≠工程锁死）；给判定写机械公式（任一不成立即难平衡）；让主对话 LLM 自行写 balance 字段；判定完成后聊/改仍保留旧结果；不推荐阻断终选；hypothesis 用 dict 分优势形态（已废弃，恒为字符串）

**沉淀 v4 提示词权威来源 (V4 Prompt Source of Truth)**
`rumination_v4_prompt.py` 的内容口径以原生 prompt 文档 `uidesign/prompt/rumination-v4new-prompt.md/rumination-v4-new.md` 为唯一权威：身份=**资深职业规划咨询师**（最后一轮咨询，温和启发式），流程/准则尽量复用 md 原文——开场白 → 2.1 匹配度确认 → 2.2 假设生成 → 2.3 平衡点验证 → 2.4 结论与收尾 + 重要准则三条（引导而非灌输/一次一问/灵活共情）。**2026-08-06 起（ADR-0015）工程协议大幅瘦身**：chips 协议（`[STEP3_HYP_JSON]`）、tool 调用协议、出卡双信号、兜底出卡**全部删除**，md 之上仅叠加——变量后置（prefix cache）、硬约束（可见正文必须/300 字/无 markdown 标题/中文/禁元话语）、收尾附加话术（找到平衡点→按假设标准复述总结→**引导用户把结论填入左侧结论卡并点确认**，可继续下一组合；未找到→建议放弃换组合）。后台 prompt 剩两个：SUMMARIZER（滚动摘要，不变）+ BALANCE_JUDGE（新增，平衡点判定；咨询师视角自由裁量、不给公式，输出 `{balance_found, balance_fail_reason}` 纯 JSON）。
_Avoid_: 从 v3 提示词（rumination_prompt_strings.py）搬运话术进 v4；把工程协议措辞混进咨询流程段落；恢复 chips/tool/双信号等已删除协议

**组合假设状态 (Combo Hypothesis State)**
每个组合有三种状态：`empty`（未填写）、`confirmed`（已确认假设）、`skipped`（已跳过，保留文字但灰显标记）。跳过分两种：纯空跳过和有字跳过（后者值得在全部提交时追问）。适用于 v3 组合矩阵格子。

**深度讨论 (Deep Discussion / filter_step=3b)**
`filter_step=3b` 回归表格+全局对话模式，表格只含已确认假设的组合（列：热爱、优势、假设），全部解锁，可选中行提问。是当前 step3 的等价替换，后续衔接 step4+ 逻辑不变。

## 支付与商业化

**试用激活码 (Trial Code)**  
注册即送、自动绑定到用户的免费码（每人仅一个，老用户无码时登录补发）。不限有效期，但**仅限 values 阶段问答且限 10 轮**（轮=用户消息条数，第 10 条正常回复、第 11 条起拦截弹购买；values 内已出结论卡要推进 strengths 时同样拦截）。LLM 档位 vip_level=1。

**完整激活码 (Full Code)**  
解锁全部五阶段+报告的码，无轮数限制，LLM 档位 vip_level=2。来源：购买套餐发**未绑定**码（2026-08-05 起不再自动绑定/自动升级）、消耗升级试用码、延期激活、管理员免费渠道。有效期**从付费升级/首次激活起算**（购买时间仅审计）。存量码（本功能上线前）一律视为完整码。
_Avoid_: 付费码、正式码

**码的双角色**  
**所属人 (Purchaser)** = 购买该码的用户（订单归属）；**激活人 (Activator)** = 实际使用该码问答的用户（claim 归属）。两者可不同（赠品码转送）。

**季度套餐 (Quarterly Package)**  
¥69：交付 1 个**未绑定**完整码（2026-08-05 起不再自动升级试用码/自动绑定），用户自由决定：消耗升级自己的试用码 / 激活成新码自用 / 转赠。有效期 3 个月；含 1 份报告。

**年度套餐 (Annual Package)**  
¥159（2026-07-28 由 ¥99 调 ¥128，2026-08-05 再调 ¥159）：交付 3 个**未绑定**完整码（有效期各自从激活起算），最多可由用户自选 1 个消耗升级自己的试用码，其余自由转赠；共 3 份报告能力，有效期 1 年；含**团队分析**能力。

**消耗升级 (Consume-to-Upgrade)**  
2026-08-05 起的试用码升级唯一路径：用户手持一个**未绑定未使用**的完整码（自己买的或被赠的均可），选择将其消耗——该码作废（不可再用不可退），已开聊的**试用激活码**原地升级为完整码（码字符串、对话记录、session 全保留）。触发入口：支付结果页弹窗（检测到有已开聊试用码时，年度 3 选 1/季度确认 1）、两种试用拦截点（10 轮上限 / values 推进被拦，提供「直购升级」与「用已有码升级」双选项）、「我的激活码」页。用被赠的码升级时，试用码所属人记为原购买者。

**延期激活 (Code Renewal)**  
对已有完整码追加有效期：ADR-0016（2026-08-08）起**任意完整码统一 ¥9.9 加 7 天，不分套餐**（旧口径 ¥20/3 个月已下线仅历史订单展示；更早分档：季度码 ¥23/3 个月、年度码 ¥33/1 年，已弃用）。从 max(当前到期时间, 现在) 起追加。买套餐永远是新码/升级，**不**自动延期旧码。另有**首次过期免费续 7 天**：每码一次，过期当天邮件+站内信送领取链接，手动领取生效（ADR-0016）。

**报告解读咨询 (Report Consultation)**  
¥298/次：一对一线上沟通 60 分钟。前置：至少一份已完成报告。购买后填预约问卷（主题/期望、候选时间段、联系方式、备注）；状态机：待填问卷→已提交→已预约→已完成。**仅未预约前可退款**。

**报告审核 (Report Review)**  
报告生成后**阻塞式审核**：用户侧显示「审核中」，admin 确认后用户才可见。兜底：固定 24 小时时限（2026-09-06 起，原随机 3~24 小时；存量 pending 不溯及既往），超时未审则**自动批复**通过——后台如实记录「自动批复」，但用户侧只感知「管理员审核通过」。计时起点（2026-07-27 起）：新建报告为 **未开始**（not_started，不计时），用户**进入报告页且五阶段已完成**时才转 **待审核** 开始 24h 倒计时，同一时刻后台预生成报告（审核期间报告已在自动生成；2026-09-06 起自动批复扫描 job 兼任生成看门狗：缓存缺失自动重试预生成，每小时一次、上限 3 次）。

**报告授权 (Report Authorization)**  
激活人在报告页一键授权，仅授权给该码的**所属人**（购买者），授权内容是**报告**；对话过程记录任何情况下不可分享、不可授权。授权开关仅对**转赠码**（所属人≠激活人）的激活人渲染；自购自用码（所属人=激活人）没有「赠送者」，报告页不显示该开关，端点也拒绝授权操作（自己的报告在团队分析中本就直接可选）。

**报告后处理修正 (Report Post-Processing)**  
报告 markdown 一次性生成后、渲染 PDF 前的可插拔修正管线（2026-08-12 起）：按章节切分，先跑确定性规则（分页标记规范化 / 列表符规范化 / **标题层级归一化**：h5/h6→h4、含中文 h4→h3），再跑针对特定章节问题的 LLM 修正器（首个为**信件压缩**：「给读者的一封信」超 ~700 字时重写至 550-650 字，配合信件区收紧排版保证信+签名一页内）；任一修正失败兜底保留原文。原生成提示词不因修正需求改动。

**报告重新生成限额 (Report Regen Limit)**  
普通用户对每份报告最多 **2 次**重新生成（2026-08-15 起）：首次生成不计数；force 重新生成**成功才计 1 次**（record.json `report_regen_count`），LLM 失败不占次数；次数用完前端按钮禁用态、后端 403。admin 不限次数不计数。三条生成触发路径（审核预生成 / 批复自动生成 / 用户手动）共用进程内**单轨锁**（`report_pdf_service._generation_inflight`），同一报告不并发重复生成；任务进行中再触发一律返回 generating。

**报告渲染引擎 (Render Engine, ADR-0019)**  
报告 markdown → PDF 的渲染器（2026-08-20 起）双引擎共存：**运行时配置 > `RENDER_ENGINE` env > 默认 weasyprint**——admin 报告页「PDF 渲染引擎」单选即时切换（简洁版 weasyprint 约 1MB / 设计版 xunlu 约 9MB），存 `data/report_render_config.json`（`report_render_config.py`）。`xunlu` = `src/report-renderer/` 精简 Node 渲染器（从设计母版 `report/xunlu` 抽取——母版只读不动；react-dom/server 出 A4 HTML + Chrome headless 打印，子进程调用，契约 markdown→bytes 不变）。分流点在 `report_pdf_service._markdown_to_pdf`，用户下载与 admin staging 预览同时生效；WeasyPrint 保留兜底。渲染器自带**输入归一化层**适配存量报告方言（旧式分页符/`*` 列表/h5-h6/章节标题层级/动态角色名）。admin 端点：`POST /admin/render-pdf-from-md`（md 直渲，不经过生成流程）/ `GET /admin/reports/{id}/render-pdf`（报告重渲染=现有 md 出新版式，与**重新生成**=LLM 出新 md 语义对立）/ `GET /admin/reports/generating`（生成中列表，前端刷新后恢复按钮态）；脚本 `scripts/rerender_report.sh <激活码>`。素材替换：母版更新后重新拷贝到 `src/report-renderer/assets/report-assets/` 即生效（即时渲染无需重生成）。

**团队分析 (Team Analysis)**  
年度套餐能力：选择多份报告（自己名下码的 + 自己订单交付且被激活人授权的）生成团队匹配度分析与团队角色投射。所属人可看到自己订单交付的所有码「被谁激活」，但看不到对话内容。

**会员 (Membership)**  
用户账户级的长期服务订阅（包月 ¥15 / 永久 ¥299 / 买码 85 折），与套餐是**并存的独立产品线**。数据库与接口保留（ADR-0006/0007），**本期不开发不展示**，后续上线首页。会员折扣对套餐的适用规则届时再定。
_Avoid_: 用「会员」指代任何套餐或码

**会员折扣 (Member Discount)**  
会员购买时自动生效的价格减免（8.5 折）。与**折扣券**是两套独立机制，可叠加：先算会员价，再减券面额，实付下限 0 元。（会员上线前不适用）

**支付订单 (Payment Order)**  
一次购买行为的单据：购买者、商品（季度/年度套餐、延期、咨询）、渠道（支付宝/微信）、金额（原价/抵扣/实付，以分存储）、状态。状态机：`pending → paid → granted`；`pending → closed`/`cancelled`；`granted → refunding → refunded`。
_Avoid_: 交易、流水、bill

**折扣券 (Coupon)**  
通用券码（不绑定用户）：一个码 + 固定金额（无门槛）+ 永久有效，**核销一次即作废**。下单时输入券码抵扣；下单即锁定、订单关闭/取消自动释放。
_Avoid_: 优惠券、代金券、promo code

**券池 (Coupon Pool)**  
全部未使用折扣券的集合。邮件发券按创建时间 FIFO 取券；池空时按系统默认金额（`DEFAULT_COUPON_AMOUNT`）自动创建补足。

**退款规则**  
码类商品（套餐/延期）：仅**未使用**（未被任何用户 claim、未**消耗升级**）的激活码可退款，退款成功即作废码；一单多码时任一码被激活/消耗，整单不可退。咨询：仅未预约前可退。退款由 admin 在后台发起（官方退款 API），用户无自助入口。

**发码自取**  
购买套餐交付的码（2026-08-05 起**全部**码，不再区分自用/赠品）：支付成功页展示 + 邮件备份，用户自行分发/激活，码不绑定购买者可转送。对比：试用码注册即自动绑定，不走此路径。

## 统计看板

**转化漏斗 (Conversion Funnel)**  
admin 看板的核心视图：浏览(PV) → 注册 → 试用开聊 → 完成10轮 → 付费 → 得到报告。各环节按**事件时间口径**统计：每个环节独立计数「该环节事件发生在选定时段内的人/次数」，不做注册队列（Cohort）回溯。

**漏斗环节定义**  
- **浏览 (PV)**：页面浏览事件，前端埋点上报，不依赖登录；匿名访客以 visitor_id cookie 标识（兼可算 UV）。
- **注册**：users 创建，按注册时间落时段。
- **试用开聊**：**试用激活码**名下发出 ≥1 条 values 用户消息（注册即自动绑定试用码，「绑定」不算「使用」）。
- **完成10轮**：values 用户消息累计达 10 条，以第 10 条发出时间落时段；不限码型。
- **付费**：**支付订单**进入 paid/granted（剔除退款单）的去重购买人数，按 paid_at 落时段。
- **得到报告**：**报告审核**通过（review_status=approved，含**自动批复**），按通过时间落时段；存量祖父豁免码无通过时间时回退用报告创建时间。

**日活跃 (Daily Active)**  
一日内的活跃事件 = 密码登录成功 + token 刷新成功（老用户保持登录态也算活跃）。「日活次数」= 事件总数，「日活人数」= 按用户去重。

**实收（纯利润）**  
选定时段内 paid/granted 且未退款订单的**实付金额**合计（Σ amount_paid，含套餐/延期/咨询全部商品）。名称沿用「纯利润」，实为实收，不含成本。

**理论收益**  
同一订单集合的**原价金额**合计（Σ amount_original，不扣折扣券抵扣）。与实收的差即折扣让利。

## 提示词分类

**阶段引导语**  
进入或离开某阶段时展示给用户的弹窗文案（intro / outro），不直接作为 LLM system prompt。

**主对话提示词**  
该阶段 LLM 对话使用的 system prompt 主体，按阶段分支。

**子步对话追加 (Chat Addon)**  
沉淀子步 1–7 各自追加到主对话 system prompt 的片段。

**子步引导语 (Step Opening)**  
进入沉淀某子步时的开场白。分两种模式：**固定文案**（直接展示）或 **LLM 生成**（由 system + user 模板驱动）。

**兜底 (Fallback)**  
主路径失败或未命中时的备用文案，例如首轮开场失败、零结果闸门、探索阶段 init 兜底等。

**运行时追加**  
对话过程中动态拼接到 system prompt 的内容（如状态协议块、使命进度、结论卡待确认摘要），非静态配置文件中的完整段落。

**Canonical 配置**  
Git 中的 YAML 与 Python 常量，生产环境默认使用的提示词来源。

**Prompt Lab 覆盖**  
仅在调试沙箱激活码下生效的提示词改写（当前限于主对话模板与 extra_goal_hint），不改变 canonical 配置。

**Effective 提示词**  
某次沙箱对话实际生效的拼装结果：canonical Layer Stack + 可选 Prompt Lab 覆盖 + 运行时注入。

**注入点 (Injection Point)**  
运行时内容拼接到 system prompt 的位置；在提示词总览中以分层堆叠视图标注顺序与触发条件。

**变量占位符 / 示例上下文**  
模板中的 `{{ variable }}`；悬停可查看固定 mock 示例，不展示真实用户数据。

## 提示词总览（Prompt Lab 扩展）

合并进 Prompt Lab，不单独开页。按五阶段浏览；沉淀阶段内按子步 1–7 展开。v1 不含 LangGraph 节点提示词。双语项（如 step_copy、chat_addon）通过页顶 **中/英切换** 展示。

## 调试与隔离

**调试沙箱 (Sandbox Fork)**  
从正式激活码 fork 出的隔离环境（`SBX*` 激活码），用于测试，不影响生产用户。

**Savepoint**  
沙箱会话在某一刻的状态快照；载入后可从该点继续手动测试，不修改 canonical 提示词文件。沉淀/子步调试时优先使用。

**试跑**  
在沙箱中验证提示词效果的两条路径：**从 Savepoint 续测**（默认/主路径）与 **Fork 从头试跑**（整阶段流程）。

## Relationships

- 一个 **组合会话** 绑定恰好 1 个热爱与 1..N 个优势
- 每个热爱/优势可带一个 **维度图标**
- **组合矩阵**（v3）的一格 ≠ 一个 **组合会话**（v4）
- **选择器胶囊** 是创建 **组合会话** 前的选点 UI，不是矩阵格子本身
- **v4 沉淀页壳** 包住顶栏、tabs、左选择器与右 AI；组合切换 / 新建组合属于大容器顶栏下一行，不属于右栏
- 「新建组合」进入 **选点草稿**；「开始探索」把草稿变成 **组合会话** 并解锁右栏
- **选点草稿** 期间右栏 AI 输入模糊锁定；已存在的 **组合会话** 被选中时右栏可输入
- **v4 结论卡区** 只跟当前会话走；换会话靠大容器内组合 tabs
- 已开聊的 **组合会话**：选择器只读锁定；改方向必须经「新建组合」回草稿
- 大容器右上角「完成并继续」与 v3 顶栏一致
- 一个 **支付订单** 至多使用一张 **折扣券**；核销成功即作废该券
- **季度套餐** 订单交付 = 1 个未绑定 **完整激活码**；**年度套餐** 订单交付 = 3 个未绑定 **完整激活码**（均发码自取，2026-08-05 起不再自动升级/自动绑定）；用户可 **消耗升级**：作废 1 个未绑定码，将已开聊 **试用激活码** 原地升级为完整码
- **延期激活** 订单只追加有效期，不交付码；**报告解读咨询** 订单交付预约问卷，不交付码
- 每个 **完整激活码** 有所属人（购买者）与激活人（使用者）两个角色；激活人可把**报告**一键授权给所属人，对话记录不可授权
- **报告** 生成后须经 **报告审核**（人工确认或超时 **自动批复**）才对用户可见
- **会员** 与套餐是并存的两条产品线（会员本期不上线）；**退款** 只发生在 **支付订单** 上，码类商品仅未使用码可退，咨询仅未预约可退

## Example dialogue

> **Dev:** "用户在 v4 里选了『帮助与影响』加三个优势，这还叫组合矩阵吗？"
> **Domain expert:** "不叫。那是一个 **组合会话**。**组合矩阵** 只指 v3 里 1×1 的格子。"

> **Dev:** "胶囊上的 ♥ 是装饰吗？"
> **Domain expert:** "不是，那是 **维度图标**，要按该热爱/优势的语义填；暂时没有语义结果时才用默认符号。"

> **Dev:** "组合 tabs 还跟 HTML 一样放在 AI 对话框上面吗？"
> **Domain expert:** "不。tabs 和「新建组合」在大容器顶栏下一行；「05 沉淀」居中，右上角是「完成并继续」。"

> **Dev:** "一进页就能在右边打字吗？"
> **Domain expert:** "不能。默认是 **选点草稿**，右边模糊锁定；点「开始探索」创建会话后才解锁。"

> **Dev:** "结论区要列出所有已保存卡吗？"
> **Domain expert:** "不要。**v4 结论卡区** 只显示当前会话那一张；其它会话用 tabs 切。"

> **Dev:** "切回已有组合后还能改热爱吗？"
> **Domain expert:** "不能。选择器只读回显；要改就点「新建组合」进草稿再「开始探索」。"

> **Dev:** "切回已有组合后还能改热爱吗？"
> **Domain expert:** "不能。选择器只读回显；要改就点「新建组合」进草稿再「开始探索」。"

> **Dev:** "会员还需要买激活码吗？"
> **Domain expert:** "**会员** 体系本期不上线，先不讨论。现在用户注册即送 **试用激活码**，限 values 10 轮；要买的是 **季度/年度套餐**，付费后试用码变 **完整激活码**。"

> **Dev:** "给 100 个人群发邮件附折扣券，是同一个码吗？"
> **Domain expert:** "不是。**折扣券** 一次核销即作废，所以从 **券池** 里给每人分一张独立码；池子空了系统自动按默认金额造新的。"

> **Dev:** "小明买了年度套餐，把赠品码送给小红，小红的报告和对话小明都能看到吗？"
> **Domain expert:** "报告可以——但得小红在报告页一键 **授权** 给所属人（小明）。对话过程记录谁都看不了，不能分享。"

> **Dev:** "小明买了年度套餐，他的试用码自动变完整码了吗？"
> **Domain expert:** "没有。3 个码全部未绑定，支付结果页弹窗问他是否 **消耗升级**——作废 1 个码换试用码原地升级；选「暂不」就全部留着自用或转赠。"

> **Dev:** "报告生成完用户就能看吗？"
> **Domain expert:** "不能，先 **报告审核**：admin 确认才可见；但有个 24 小时的固定兜底（2026-09-06 起，原 3~24 小时随机），超时 **自动批复** 通过，用户只知道『管理员审核通过』。"

> **Dev:** "用户激活码已经用了一半，能退款吗？"
> **Domain expert:** "不能。**退款规则** 是仅未使用的码可退；咨询则是未预约前可退。"

## Flagged ambiguities

- 「矩阵 / matrix」曾同时指 v3 的 3×5 格子与 v4 选择区 — 已拆成 **组合矩阵** vs **组合会话** + **选择器胶囊**。
- HTML mock 的紫大卡片 ≠ 产品要的视觉；视觉以 v3 橙/绿胶囊与毛玻璃为准。
- HTML mock 把 combo-tabs 放在右栏 — 已否决；见 **v4 沉淀页壳**（tabs 在大容器顶栏下一行）。
- 「新建组合」≠「开始探索」：前者进草稿，后者才创建并解锁聊天。
- HTML「已保存 N 张」列表 — 已否决；见 **v4 结论卡区**。
- 已开聊会话上原地改热爱/优势 — 已否决；组合固定，改方向走新建。
- 整页紫强调 — 已否决；仅「开始探索」保留紫 CTA，其余跟 v3。
- 大容器右上角需要「完成并继续」（与 v3 顶栏一致）；不再「顶栏仅标题」。
- 「会员买码」曾被理解为会员免码或码延期 — 已否决：**会员** 与 **激活码** 是两条产品线，会员仅享折扣与高级模型固化。（会员本期不上线）
- 「折扣券」曾被讨论为绑定用户/百分比/满减 — 已否决：通用码、固定金额、无门槛、永久有效、核销一次即作废。
- 「发码自取」曾被讨论为自动绑定购买账户 — 已否决：套餐码**发码自取**可转送；但**试用激活码**相反，注册即自动绑定。
- 「购买套餐自动升级试用码 / 自用码自动绑定购买者」— 已否决（2026-08-05）：套餐全部交付**未绑定**码，由用户自主选择 **消耗升级** 试用码、激活自用或转赠；拦截点直购（用户主动发起）除外，支付成功即自动消耗升级。
- 「连续包月」曾被讨论为到期提醒手动续费 — 已否决：按真实代扣（委托代扣/周期扣款）设计，但会员功能本期不开放。
- 「全程激活码 ¥99/180 天」单一 SKU — 已下架：商品目录改为 **季度套餐/年度套餐/延期激活/报告解读咨询**（2026-07-20）。
- 「再买套餐给原码延期」— 已否决：买套餐永远是新码/升级试用码；旧码续命走 **延期激活**（ADR-0016 起统一 ¥9.9/7 天，首次过期可免费领 7 天）。
- 「团队分析共享对话记录」— 已否决：只共享**报告**（且需激活人一键授权给所属人），对话记录不可分享。
