# ==========================================================
# 测试环境覆盖配置
# 用法: ./start.sh start test
# 加载顺序: .env (base) → .env.test (覆盖)
# ==========================================================

APP_ENV=test
DEBUG=True
DEBUG_MODE=True

# 数据库（测试环境）
DATABASE_URL=sqlite+aiosqlite:///./app.test.db

# DeepSeek Key（可选覆盖）：留空 = 继承 .env 的测试 key；
# 如需为本环境单独换 key 在此填写（2026-08-19 起测试/生产 key 完全隔离）
# DEEPSEEK_API_KEY=

# 前端
FRONTEND_URL=http://47.96.75.69:3000
NEXT_PUBLIC_API_URL=
FRONTEND_MODE=dev

# Admin 调试
ADMIN_DEBUG_POLICY_ENABLED=True
ADMIN_DEBUG_WORKSPACE_ENABLED=True
ADMIN_SANDBOX_ENABLED=True

# 账户注销冷存天数（测试环境设小数值，便于验证到期清除）
ACCOUNT_DELETION_RETENTION_DAYS=0.01
